import React from 'react';
import {Link }from '@reach/router';

export default function Navbar() {
  return (
    <nav>
      <ul>
        <Link to="ipocalendar">Ipo</Link>
      </ul>
    </nav>
  )
}
